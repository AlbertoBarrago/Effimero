services:
  effimero:
    build: .
    ports:
      - "3000:3000"
    environment:
      REDIS_URL: redis://redis:6379
      TRUST_PROXY: "${TRUST_PROXY:-false}"
      ALLOWED_ORIGINS: "${ALLOWED_ORIGINS:-*}"
      RETENTION_DAYS: "${RETENTION_DAYS:-90}"
      COLLECT_RATE_LIMIT: "${COLLECT_RATE_LIMIT:-120}"
      COLLECT_RATE_WINDOW: "${COLLECT_RATE_WINDOW:-60}"
      MAX_DISTINCT_PATHS: "${MAX_DISTINCT_PATHS:-2000}"
      MAX_DISTINCT_REFERRERS: "${MAX_DISTINCT_REFERRERS:-2000}"
      STATS_API_KEY: "${STATS_API_KEY:-}"
    depends_on:
      - redis
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: redis-server --save 300 1 --appendonly no
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  redis-data:
